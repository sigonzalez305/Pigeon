# Pigeon Messenger

A minimal web-based messenger that delivers messages with a random delay (up to five minutes). This repository contains static files only.

Open `index.html` in your browser to try it out.
